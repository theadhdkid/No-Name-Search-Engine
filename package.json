{
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "dev": "nf start --procfile Procfile.dev"
  },

  "dependencies": {
    "@fastify/autoload": "^6.2.0",
    "@prisma/client": "^6.3.1",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "prettier": "3.4.2",
    "prisma": "^6.3.1"
  }
}
